export const panel = {
    position: "fixed",
    left: 0,
    right: 0,
    bottom: 24,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    pointerEvents: 'none',
    gap: 4,
    zIndex: 0,
} as const;
